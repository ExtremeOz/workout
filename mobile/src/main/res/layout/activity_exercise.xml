<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical"
    android:id="@+id/container"
    tools:context=".activity.ExerciseActivity">
   <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:elevation="@dimen/item_elevation"
        style="@style/ToolbarAppearance"
        android:popupTheme="@style/ThemeOverlay.AppCompat.Light">
   </androidx.appcompat.widget.Toolbar>
    <FrameLayout
        android:id="@+id/exercise_placeholder"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!--<LinearLayout
        android:elevation="4dp"
        android:id="@+id/bottom_bar"
        style="@style/LayoutAppearance.BottomBar">
        <com.google.android.material.button.MaterialButton
            android:id="@+id/exercise_cancel_button"
            style="@style/ButtonAppearance.BarButton"
            android:text="@string/cancel_button_text"
            android:layout_marginEnd="15dp"
            android:layout_marginBottom="0dp" />
        <com.google.android.material.button.MaterialButton
            android:id="@+id/exercise_save_button"
            style="@style/ButtonAppearance.BarButton"
            android:text="@string/save_button_text" />
        <com.google.android.material.button.MaterialButton
            android:id="@+id/exercise_send_button"
            style="@style/ButtonAppearance.BarButton"
            android:visibility="gone"
            android:text="@string/send_button_text" />
    </LinearLayout>-->
</LinearLayout>