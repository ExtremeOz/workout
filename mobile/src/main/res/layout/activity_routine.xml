<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical"
    android:id="@+id/container"
    android:background="@color/primaryDarkColor"

    tools:context=".activity.RoutineActivity">
   <androidx.appcompat.widget.Toolbar
       android:id="@+id/toolbar"
       style="@style/ToolbarAppearance"
       android:popupTheme="@style/ThemeOverlay.AppCompat.Dark"/>
    <FrameLayout
        android:id="@+id/workout_placeholder"
        android:layout_below="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />
    <!--
        <LinearLayout
            android:elevation="4dp"
            android:id="@+id/workout_bottom_bar"
            style="@style/LayoutAppearance.BottomBar">
            <com.google.android.material.button.MaterialButton
                android:id="@+id/workout_cancel_button"
                style="@style/ButtonAppearance.BarButton"
                android:text="@string/cancel_button_text"
                android:layout_marginEnd="15dp"
                android:layout_marginBottom="0dp" />
            <com.google.android.material.button.MaterialButton
                android:id="@+id/workout_save_button"
                style="@style/ButtonAppearance.BarButton"
                android:text="@string/save_button_text" />
            <com.google.android.material.button.MaterialButton
                android:id="@+id/workout_send_button"
                style="@style/ButtonAppearance.BarButton"
                android:text="@string/send_button_text" />
    </LinearLayout>
    -->
</LinearLayout>